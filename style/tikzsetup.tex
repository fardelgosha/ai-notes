\RequirePackage{tikz}
\usetikzlibrary{arrows,positioning}

\tikzset{
    arrow/.style={
            ->,
            >=stealth',
            line width=1pt
        },
    sum/.style={
            circle,
            draw,
            minimum size=8mm,
            inner sep=0pt
        },
    block/.style={
            rectangle,
            draw,
            minimum width=18mm,
            minimum height=10mm
        }
}